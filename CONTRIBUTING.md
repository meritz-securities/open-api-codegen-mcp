# 기여 안내

메리츠증권 Open API 코드 생성 MCP 에 관심 가져 주셔서 감사합니다.
이 문서는 이슈를 올리시거나 코드를 보내실 때 참고하실 내용을 담았습니다.

## 먼저 확인해 주십시오

**계좌·주문·거래에 관한 문의는 이곳에서 처리해 드릴 수 없습니다.**

- 주문이 나가지 않았다, 잔고나 평가금액이 다르게 보인다, 체결 내역을 확인하고 싶다
- 앱키 발급, 계좌 연결, 권한 부족
- API 명세·업무 규칙에 대한 문의

이런 내용은 **개발자 포털의 문의 채널**을 이용해 주십시오. 이 서버는 코드를 만들어
드릴 뿐 API 를 직접 호출하지 않으며, 저장소 관리자에게는 계좌를 조회하거나 주문
상태를 확인할 권한이 없습니다.

이곳에서 다루는 것은 **이 서버가 만들어 주는 코드와 명세**에 관한 문제입니다.

## 자격증명과 계좌 정보

이 서버 자체는 앱키를 쓰지 않습니다. 다만 **만들어진 코드를 실행해 보신 결과**를
옮기실 때 자격증명이나 계좌 정보가 함께 딸려 오기 쉽습니다.

이슈·PR·커밋에 다음을 절대 넣지 말아 주십시오.

- 앱키(App Key)·시크릿(App Secret)·접근토큰
- 계좌번호
- 실행 결과에 담긴 잔고·보유종목·주문번호 등 실제 값

이슈는 누구나 볼 수 있고, 지우셔도 수정 이력에 남습니다.
이미 노출하셨다면 개발자 포털에서 **즉시 재발급**해 주십시오.

## 이슈

이슈 양식을 골라 주십시오. 빈 이슈는 열어 두지 않았습니다.

| 양식 | 언제 |
|---|---|
| 만들어 준 코드나 검색이 이상합니다 | 코드 생성·검색·명세 조회에서 생긴 문제 |
| 명세·문서가 실제와 다릅니다 | 도구가 알려 준 명세나 README 가 실제와 어긋날 때 |
| 이런 기능이 있었으면 합니다 | 개선 제안 |

**어느 클라이언트인지(Claude Desktop·Claude Code·Cursor·VS Code·Codex CLI)**,
부르신 도구와 넘기신 `api_type`, 서버 버전, 파이썬 버전을 적어 주시면 확인이
훨씬 빠릅니다.

## 개발 환경

Python 3.11 이상과 [uv](https://docs.astral.sh/uv/) 가 필요합니다.
이 서버를 개발하시는 데에는 앱키가 필요 없습니다.

```bash
git clone https://github.com/meritz-securities/open-api-codegen-mcp.git
cd open-api-codegen-mcp
uv sync
```

## 테스트

```bash
uv run pytest tests/ -q
```

네트워크 없이 돕니다. `tests/test_codegen.py` 는 생성물이 문법적으로 올바른지,
확인 단계와 응답 판정이 약속대로 들어가는지를 봅니다. 생성된 코드를 실행해
검증하기 때문에 개발 의존성에 `requests` 가 들어 있습니다.

**테스트를 위해 실제 API 를 호출하지 말아 주십시오.** 앱키에는 실계좌가 연결되어
있습니다.

## 코드를 보내실 때

만들어진 코드는 사용자의 실제 계좌를 향해 실행됩니다. 아래 약속은 안전 장치이므로
지켜 주십시오.

- **주문·환전 코드의 확인 단계를 빼지 말아 주십시오.** `confirm=True` 를 넘기기
  전에는 전송하지 않는 형태를 유지합니다
- 주문 코드의 `warn_cls_code` 검사는 `rsp_cd` 성공 반환보다 **앞에** 두어야 합니다.
  뒤에 두면 걸리지 않습니다
- 생성물이 HTTP 200 을 그대로 성공으로 넘기지 않도록 해 주십시오
- 앱키·시크릿을 코드에 값으로 박지 말고 환경변수로 읽게 해 주십시오
- 실제 계좌번호나 실제 금액을 예시 값으로 쓰지 말아 주십시오
- 코드 스타일은 `pyproject.toml` 의 ruff 설정을 따릅니다 (줄 길이 120).
  코드 템플릿에서 `%` 포맷을 쓰는 것은 의도한 것입니다 — f-string 으로 바꾸면
  생성물의 중괄호와 섞입니다

### 명세를 고치실 때

명세는 포털에 등록된 내용을 그대로 따르는 것이 원칙입니다. 포털 예시와 실제 동작이
다른 지점만 `meritz_codegen/data/corrections.json` 에 적고, **무엇을 어떻게 확인하셨는지**
근거를 함께 남겨 주십시오.

## 브랜치와 PR

1. 저장소를 포크하고 브랜치를 만들어 주십시오
2. 변경 후 `uv run pytest tests/ -q` 를 돌려 주십시오
3. PR 양식을 채워 보내 주십시오

## 보안 취약점

생성되는 코드에 자격증명이 남거나 확인 절차가 빠지는 등 안전에 관한 내용은
**공개 이슈나 PR 로 올리지 마시고**, [SECURITY.md](SECURITY.md) 에 적힌 절차를
따라 주십시오.

## 라이선스

기여하신 내용은 이 저장소와 같은 [MIT 라이선스](LICENSE)로 배포됩니다.
