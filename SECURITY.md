# 보안 정책

메리츠증권 Open API 저장소의 취약점 신고 및 App Key 관리 정책입니다.

## 취약점 신고

인증 우회, 자격증명 노출, 권한 상승과 관련된 취약점을 발견한 경우 공개 이슈를 등록하지 말고 개발자 포털의 문의 채널로 신고하십시오.

- 개발자 포털: https://openapi.imeritz.com

신고 시 재현 절차와 예상 영향 범위를 함께 제공해 주시기 바랍니다.

## 이 서버는 앱키를 다루지 않습니다

코드를 만들기만 하고 API 를 호출하지 않으므로 자격증명이 필요 없습니다.
**이 서버에 앱키를 설정하지 마십시오.** 생성된 코드는 실행하는 쪽 환경변수
(`MERITZ_APP_KEY`, `MERITZ_APP_SECRET`)에서 값을 읽습니다.

## 생성된 코드 유의

- 생성물의 기본 대상은 **운영 서버**(`openapi.imeritz.com:9443`)입니다.
- 주문 코드에는 확인 단계가 들어 있습니다. **그 단계를 지우고 쓰지 마십시오.**
- 실행 전에 요청 본문을 직접 확인하십시오.

## 원격 MCP 연결

이 서버는 자격증명을 보관하지 않고 API 를 호출하지도 않으므로, 노출되어도 계좌에
접근되지 않습니다. 다만 Streamable HTTP 의 로컬 포트를 그대로 공개하지는 마십시오.
외부에서 접근해야 한다면 주소에 난수 경로를 넣고, 가능하면 인증·TLS 가 적용된
프록시 뒤에 두십시오.
